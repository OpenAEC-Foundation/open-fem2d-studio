# Plan: platen als dragende wandschijven (fase 1)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.
> Bron: vier verkenningsrapporten platen d.d. 2026-09-02 (solverkern, v2-UI-keten,
> v1-erfenis, functionele eisen). Alle regelnummers hieronder zijn geverifieerd in
> de huidige master.

**Doel:** een getekende plaat rekent Ã©cht mee. Vandaag is de plaat in v2 decoratie:
`Plate {id, nodeIds}` (femTypes.ts:97-100) zonder dikte of materiaal, de engine lost
altijd op met `analysisType: "frame"` (engine.ts:387/401/496) en het enige rekeneffect
zijn de vier stilzwijgend toegevoegde randstaven (default HEA160/S235) â€” misleidend
voor de gebruiker. Na dit plan: plaat met dikte/materiaal/meshSize, meegenomen in de
multi-LC-pijplijn als membraan naast de staven, spanningscontouren op het canvas,
randlasten en eigengewicht, polygonen via randconforme CDT, en platen in tabel en
rapport.

## Architectuurkeuze: wandschijf (in het vlak) eerst

Fase 1 implementeert de plaat als **membraan/wandschijf (plane stress) in
`mixed_beam_plate`**, niet als buigplaat. Argumenten:

1. **Past in het 2D-model.** De hele tool leeft in Ã©Ã©n vlak (raamwerken, x/z).
   Een wandschijf (stabiliteitswand, wandligger, lijfplaat, ligger-op-wand) is het
   plaatgeval dat natuurlijk in datzelfde vlak en dezelfde resultatenwereld valt.
   Plaatbuiging (vloer, uit het vlak) is een tweede wereld met eigen DOF's (w/Î¸x/Î¸y),
   eigen resultaten (mx/my/mxy) en eigen randvoorwaarden â€” feitelijk een apart
   documenttype.
2. **Het schijfpad is het enige kwantitatief geverifieerde plaatpad in de core.**
   CST/Quad4 + assemblage + `solvePlateOrPlane`/`solveMixed` bestaan al en de
   verkenning valideerde ze numeriek (trekstaaf u/u_analytisch = 1,0029, Ïƒx â‰ˆ 5,0
   N/mmÂ² zoals verwacht). Het DKT-buigelement is daarentegen aantoonbaar defect
   (divergeert bij meshverfijning; zes exact gelokaliseerde foutieve termen).
3. **Samenwerking met staven bestaat alleen voor membranen.** `solveMixed`
   combineert staven (6Ã—6) met membraanelementen (3 DOF/knoop, hardcoded
   plane_stress); buiging doet in mixed-mode niet mee. Een vloer-op-balken-scenario
   zou dus Ã³Ã³k na een DKT-fix nieuwbouw in de solver vergen.

De DKT-fix zelf landt wÃ©l (taak P0.2): hij is klein, exact bekend en voorkomt dat
bewezen-foute code blijft liggen. UI-ontsluiting van plaatbuiging blijft backlog.

**Hergebruik, niet herbouwen.** `design-mockup/src/core` bevat de plaatstack al
byte-identiek uit v1: `generatePlateRegionMesh` (grid), `generatePolygonPlateMeshV2`
(CDT), `convertEdgeNodeIdsToNodalForces`, Triangle/Quad4/DKT, Assembler-paden en
`solveMixed`. Er wordt dus niets gekopieerd; het plan sluit aan en repareert.
V1's MeshEditor-contourrendering (MeshEditor.tsx:2936-3247) en PlateDialog dienen
alleen als referentie â€” de v1-canvas-orkestratie is niet overneembaar.

## Randvoorwaarden

- Nederlandse UI-teksten en comments; **geen productnamen van externe rekensoftware**.
- `npx tsc --noEmit` schoon in design-mockup **per taak**.
- **Analytisch-exacte tests** in de stijl van de bestaande `test-*.mjs` (top-comment
  met referenties, `check(name, actual, expected, tolPct)`, exit-code op falen),
  uitgevoerd met `node test-<naam>.mjs` vanuit design-mockup.
- **Geen regressie op de bestaande 16 solver-tests**: test-checkconfig, test-deellast,
  test-doorbuiging-toets, test-leeg-geval, test-n-teken, test-pendelstaaf,
  test-releases, test-scheefstand, test-sectie-doorvoer, test-splitsen, test-thermiek,
  test-transform, test-tweede-orde, test-v2-stations, test-veldzakking, test-veren.
- Commit per afgeronde taak op master.
- Elke taak noemt **verboden bestanden** (niet aanraken) zodat taken conflictvrij
  parallel of door subagents kunnen lopen.

## Ontwerpbesluiten (gelden voor het hele plan)

- **Eenheden zoals staven:** UI canoniek mm/kN â†’ adapter naar core-SI: coÃ¶rdinaten
  mmâ†’m (engine.ts:119), E N/mmÂ²â†’Pa (Ã—1e6, engine.ts:107), dikte mmâ†’m, lasten kNâ†’N;
  spanningen terug Paâ†’N/mmÂ².
- **Mesh per solve, geen mutatiegevoel:** de engine bouwt per belastinggeval een
  verse `Mesh` (engine.ts:394). Het rechthoekige grid (`generatePlateRegionMesh`,
  deterministisch en synchroon) wordt gewoon per solve gegenereerd â€” daarmee is de
  bekende mesh-mutatie door `solvePlateOrPlane` (constraint/last-transfer,
  NonlinearSolver.ts:1116-1134) geen risico. Alleen het async CDT-pad (fase P4)
  krijgt een cache in de store + projectbestand.
- **Elementkeuze grid:** Quad4 (`elementType: 'quad'`) voor rechthoeken â€” regelmatig
  grid, geen detJ-problemen, beter buiggedrag dan CST. Het CDT-pad levert per
  definitie driehoeken (CST â€” stijver; fijnere mesh nodig, gedocumenteerd in de UI).
- **Analysetype:** zodra het model â‰¥ 1 plaat bevat â†’ `mixed_beam_plate`
  (dispatch NonlinearSolver.ts:754), anders ongewijzigd `frame`. Let op:
  `solveMixed` heeft gÃ©Ã©n constraint-transfer (error op losse knopen,
  NonlinearSolver.ts:1444-1446) â€” de adapter valideert dat.
- **Schaalbewaking:** dense Gauss-eliminatie is O(nÂ³)/O(nÂ²)-geheugen
  (GaussElimination.ts). De adapter weigert modellen boven Â±4000 DOF's met een
  nette NL-melding ("meshSize vergroten"); sparse solver staat op de backlog.
- **Auto-randstaven verdwijnen:** de plaattool voegt geen 4 verborgen HEA160-staven
  meer toe (FemCanvas.tsx:872-876) â€” de plaat draagt nu zelf. Bestaande projecten
  behouden hun expliciete staven.

---

## Fase P0 â€” Kern bewijzen en repareren

- [x] **P0.1 Analytisch bewijs schijfpad vastleggen.** De verkenning verifieerde
  CST/Quad4 in scratch-scripts; dat bewijs hoort als test in de repo. Testinhoud:
  (a) **patchtest** â€” lineair verplaatsingsveld op een onregelmatig CST/Quad4-lapje
  wordt exact gereproduceerd (relatieve fout < 1e-8); (b) **trekstaaf** 1,0Ã—0,2 m,
  t=10 mm, Ïƒ=5 N/mmÂ², 10Ã—2-mesh via `generatePlateRegionMesh` + `solveNonlinear`
  (`plane_stress`): u_eind = ÏƒL/E binnen 0,5%, Ïƒx per element binnen 6% van 5,0
  N/mmÂ², Î£R = ÏƒÂ·bÂ·t binnen 0,1%. Beide voor triangle Ã©n quad.
  *Bestanden:* Create `design-mockup/test-plaat-schijf.mjs`. Geen productiecode â€”
  faalt de test, dan stoppen en rapporteren (dan klopte de verkenning niet).
  *Niet aanraken:* alle bestanden onder `design-mockup/src/`.

- [x] **P0.2 DKT-buigelement repareren (6-termen-fix).** In `computeDKTBMatrix`
  staan de afgeleiden van de kwadratische hoekvormfuncties Lk(2Lkâˆ’1) hardcoded als
  Â±1 in plaats van Â±(4Lkâˆ’1): DKT.ts:108/122/136 (`1 - â€¦` â†’ `(4*L1-1) - â€¦` resp.
  L2/L3) en DKT.ts:142/156/170 (`-1 + â€¦` â†’ `-(4*L1-1) + â€¦` resp. L2/L3). Corrigeer
  ook het header-commentaar (DKT.ts:7): de werkelijke DOF-conventie nÃ¡ fix is
  [w, âˆ’âˆ‚w/âˆ‚y, +âˆ‚w/âˆ‚x]. Testinhoud: (a) **rigid-body**: drie starre-lichaamsmodes
  geven â€–KeÂ·uâ€–âˆž < 1e-8Â·â€–Keâ€–âˆž; (b) **constante kromming**: patch onder zuivere
  kromming geeft my/mx = Î½ binnen 1%; (c) **vrij opgelegde vierkante plaat** met
  centrale puntlast P, Î½=0,3, n=12: w_mid vs analytisch 0,01160Â·PÂ·aÂ²/D binnen 1,5%,
  met monotone convergentie over n=4/8/12 (verkenning: ratio 1,057â†’1,018â†’1,009).
  Momenttekens vastleggen in het testcommentaar.
  *Bestanden:* Modify `design-mockup/src/core/fem/DKT.ts`; Create
  `design-mockup/test-plaat-dkt.mjs`.
  *Niet aanraken:* Triangle.ts, Quad4.ts, Assembler.ts, NonlinearSolver.ts,
  engine.ts, alle UI-bestanden.

- [x] **P0.3 Knoop-id-conventie beschermen.** `Mesh.fromJSON` zet `nextNodeId` op
  het maximum over Ã¡lle knopen inclusief plaatknopen (Mesh.ts:1000); na laden van
  een model met plaatknopen (id â‰¥ 1000) krijgen nieuwe reguliere knopen dan ook
  id's â‰¥ 1000 en botsen ze met `nextPlateNodeId`. Fix: `nextNodeId` alleen over
  id < 1000 bepalen. Testinhoud (exact): mesh met reguliere knopen + plaatknopen â†’
  toJSON â†’ fromJSON â†’ `addNode` geeft een id < 1000 aansluitend op de reguliere
  reeks; `addPlateNode` geeft â‰¥ 1000 zonder botsing met bestaande ids.
  *Bestanden:* Modify `design-mockup/src/core/fem/Mesh.ts`; Create
  `design-mockup/test-plaat-ids.mjs`.
  *Niet aanraken:* alle overige core-, solver- en UI-bestanden.

## Fase P1 â€” Kern: wandschijf in mixed_beam_plate

- [x] **P1.1 Mixed-kern analytisch bewijzen.** Rechtstreeks op de core (Mesh +
  `solveNonlinear` met `mixed_beam_plate`), nog zonder adapter. Testinhoud:
  (a) **trekwand**: kolomschijf b=1 m, h=3 m, t=20 mm, quad-grid, onderrand alle
  knopen vast, bovenrand knooplasten volgens tributary lengths â†’ u_top = Ïƒh/E
  binnen 1%, Ïƒy per element binnen 2%, Î£R = totale last binnen 0,1%;
  (b) **kraagwand** L=4 m, h=1 m, t=100 mm (L/h=4), tiplast â†’ w_tip vs Timoshenko
  PLÂ³/3EI + 1,2Â·PL/(GA) binnen 5% bij 32Ã—8 quads, en mesh-verdubbeling verkleint
  de fout (monotone convergentie); (c) **staaf-equivalentie**: een los raamwerk
  (ligger + q-last) met een ver weg gelegen, apart opgelegd mini-plaatje geeft in
  mixed-mode dezelfde liggerresultaten als in frame-mode binnen 0,1% â€” bewijst dat
  de rotatiestabilisatie (Assembler.ts:321) staafresultaten niet vervuilt.
  Gevonden bugs worden Ã­n deze taak gefixt (core), met vermelding in het
  testcommentaar.
  *Bestanden:* Create `design-mockup/test-plaat-mixed.mjs`; Modify (alleen bij
  bewezen bug) `design-mockup/src/core/solver/Assembler.ts` en/of
  `NonlinearSolver.ts`.
  *Niet aanraken:* engine.ts, femTypes.ts, alle UI-bestanden, DKT.ts.

- [x] **P1.2 Plaatlasten-helpers in de kern.** Nieuw bestand `PlateLoads.ts` met:
  (a) **eigengewicht** membraanelementen â†’ knooplasten (CST: ÏÂ·gÂ·tÂ·A/3 per knoop;
  Quad4: ÏÂ·gÂ·tÂ·A/4), (b) dunne wrapper om de bestaande randlast-conversie
  `convertEdgeNodeIdsToNodalForces` (PlateRegion.ts:289, cumulatieve booglengte).
  Testinhoud (exact, het zijn sommen): Î£Fz eigengewicht = ÏÂ·gÂ·tÂ·A_totaal binnen
  1e-9 relatief; randlast Î£F = pÂ·L binnen 1e-9 relatief met trapeziumverdeling
  (randknopen Â½-gewicht); integratie: trekwand belast via de randlast-helper â†’
  u_top = Ïƒh/E binnen 1%.
  *Bestanden:* Create `design-mockup/src/core/fem/PlateLoads.ts`,
  `design-mockup/test-plaat-lasten.mjs`.
  *Niet aanraken:* PlateRegion.ts (alleen importeren), Assembler.ts,
  NonlinearSolver.ts, alle UI-bestanden.

## Fase P2 â€” Datamodel en adapter (engine-doorvoer)

- [x] **P2.1 Plate-datamodel + persist.** `Plate` krijgt optionele velden met
  defaults: `thickness` (mm, default 20), `E` (N/mmÂ², default 210000), `nu` (0,3),
  `rho` (kg/mÂ³, 7850), `meshSize` (mm, default 500). Store: `addPlate` vult
  defaults, nieuwe `updatePlate(id, updates)` naar analogie van `updateBeam`
  (useFemStore.ts:621-627). Persist via het bestaande optionele-velden-patroon
  (zoals scheefstand, projectFile.ts:18-21) â€” **geen versie-bump**; versie-
  geschiedeniscommentaar bijwerken. Testinhoud: round-trip
  `serializeProject`/`deserializeProject` behoudt de vijf velden exact; een
  projectbestand zÃ³nder die velden laadt met de defaults.
  *Bestanden:* Modify `design-mockup/src/components/fem/femTypes.ts`,
  `design-mockup/src/hooks/useFemStore.ts`, `design-mockup/src/io/projectFile.ts`;
  Create `design-mockup/test-plaat-project.mjs`.
  *Niet aanraken:* engine.ts, solver/types.ts, App.tsx, FemCanvas.tsx,
  FemProperties.tsx, alle core-bestanden.

- [x] **P2.2 Adapter: platen de engine in, spanningen eruit.** `SolverInput`/
  `MultiInput` krijgen `plates` (solver/types.ts:120-148); `buildMesh`
  (engine.ts:87): per plaat asgelijnde-rechthoek-validatie (4 hoeken, binnen
  tolerantie; gedraaid/degeneraat â†’ duidelijke NL-fout), divisions uit meshSize,
  quad-grid via `generatePlateRegionMesh` (hoek- en randknopen hergebruiken via
  het bestaande `findNodeAt`-pad), per plaat een mesh-materiaal (E, Î½, Ï),
  DOF-bewaking Â±4000 met nette melding. Analysetype `mixed_beam_plate` zodra
  platen aanwezig; validatie vooraf dat elke UI-knoop met steunpunt of last op een
  meshknoop valt (anders NL-fout â€” `solveMixed` transfereert niet).
  `convertResult`: verplaatsingen van plaatknopen (id â‰¥ 1000) meenemen in
  maxDisplacement, en nieuw resultaatveld `plateElements` per plaat: per element
  knoopcoÃ¶rdinaten + Ïƒx, Ïƒy, Ï„xy, vonMises, Ïƒ1/Ïƒ2/hoek, nx/ny/nxy (uit het
  bestaande mixed-postprocessingpad, NonlinearSolver.ts:1557-1615) + min/max-
  ranges. Testinhoud: via `solveAllCases`: trekwand 3Ã—3 m t=20 mm met UI-knopen
  op de bovenrand-gridposities (test meteen knoop-hergebruik) â†’ u_top = Ïƒh/E
  binnen 1%; **dikte Ã—2 â†’ Ïƒ exact Ã—0,5** (binnen 0,1%); model zonder platen â†’
  gedrag bit-identiek (alle 16 bestaande tests groen).
  *Bestanden:* Modify `design-mockup/src/components/fem/solver/types.ts`,
  `design-mockup/src/components/fem/solver/engine.ts`; Create
  `design-mockup/test-plaat-adapter.mjs`.
  *Niet aanraken:* App.tsx, FemCanvas.tsx, FemProperties.tsx, useFemStore.ts,
  alle core-bestanden (alleen importeren).

- [x] **P2.3 App-doorvoer + plaat-eigengewicht.** `computeAndStoreSolverOutputs`
  (App.tsx:512-599) geeft `fem.plates` door in de MultiInput; bij
  `selfWeightEnabled` krijgt de plaat-invoer een `selfWeightCaseId` (dead case,
  zelfde keuze als staven, App.tsx:547) en zet `buildMesh` dat via `PlateLoads`
  om in knooplasten op de meshknopen. Testinhoud: wandschijf 3Ã—3 m t=200 mm,
  Ï=2500 (invoervelden), alleen eigengewicht â†’ Î£Rz = ÏÂ·gÂ·tÂ·A binnen 0,1%
  (engine-level test, uitbreiding van test-plaat-adapter.mjs of apart bestand).
  *Bestanden:* Modify `design-mockup/src/App.tsx`,
  `design-mockup/src/components/fem/solver/types.ts` (veld),
  `design-mockup/src/components/fem/solver/engine.ts` (doorvoer); Create
  `design-mockup/test-plaat-gewicht.mjs`.
  *Niet aanraken:* FemCanvas.tsx, FemProperties.tsx, core-bestanden.

- [x] **P2.4 Staven langs plaatranden laten samenwerken.** (a) Plaattool stopt met
  de vier auto-randstaven (FemCanvas.tsx:872-876) â€” melding in het HUD blijft.
  (b) `buildMesh` splitst een staaf die exact op een plaatrand ligt op de
  plaatrandknopen (anders hangt de plaat alleen aan de staafuiteinden); de
  beamIdMap wordt dan 1â†’n en `convertResult` rijgt de stations van de deelstukken
  aaneen tot Ã©Ã©n staafresultaat (N/V/M/w continu, L totaal). Testinhoud:
  (1) wandschijf + randstaaf met verwaarloosbare stijfheid (EÃ—1e-6) â‰¡ schijf
  zonder staaf binnen 0,5% (u_top en Ïƒ); (2) stijve ligger Ã³p een wand met
  puntlast: Î£R = P binnen 0,1% en w_ligger ligt tussen de analytische
  ligger-alleen- en wand-alleen-waarden; (3) staaf nÃ­et op een plaatrand â†’
  ongesplitst, bestaande 16 tests groen.
  *Bestanden:* Modify `design-mockup/src/components/fem/FemCanvas.tsx` (alleen de
  addPlate-tak), `design-mockup/src/components/fem/solver/engine.ts`; Create
  `design-mockup/test-plaat-randstaaf.mjs`.
  *Niet aanraken:* App.tsx, FemProperties.tsx, useFemStore.ts, core-bestanden.

## Fase P3 â€” UI: eigenschappen, contouren, randlast

- [ ] **P3.1 Plaateigenschappen echt + tool-validatie.** `PlateProperties`
  (FemProperties.tsx:686-716) vervangt de stub (dood S235-select, hardcoded
  "20 mm") door bewerkbare velden dikte/E/Î½/Ï/meshSize â†’ `updatePlate` â†’
  herberekening (zelfde trigger-route als staafwijzigingen). De tekentool weigert
  degeneratie: collineaire of samenvallende hoekklikken en niet-asgelijnde
  rechthoeken geven een statusbalkmelding in plaats van een kapotte plaat.
  *Test:* tsc schoon + handmatige checklist (dikte wijzigen â†’ Ïƒ halveert zichtbaar
  bij Ã—2; ongeldige vorm geweigerd met melding; undo/redo werkt over updatePlate).
  *Bestanden:* Modify `design-mockup/src/components/fem/FemProperties.tsx`,
  `design-mockup/src/components/fem/FemCanvas.tsx` (validatie in de addPlate-tak).
  *Niet aanraken:* engine.ts, App.tsx, TableView.tsx, core-bestanden.

- [ ] **P3.2 Spanningscontouren op het canvas.** Resultaatmodus: elementvlakken
  gevuld op gekozen component (von Mises default; Ïƒx/Ïƒy/Ï„xy/nx/ny/nxy via een
  keuze in de bestaande resultaten-toolbar), kleurenlegenda met min/max uit de
  `plateElements`-ranges, mesh-lijnen-toggle. V1-rendering
  (MeshEditor.tsx:2936-3247) alleen als referentie; implementatie in de
  bestaande FemCanvas-lagenstructuur (renderPlate-laag, FemCanvas.tsx:1578-1596).
  Let op de bekende valkuil: `viewStateRef.current` gebruiken in drawfuncties.
  *Test:* tsc schoon + handmatig: trekwand toont uniforme kleur (Ïƒy-spreiding
  < 2% van de legenda-range), kraagwand toont het verwachte trek/druk-beeld;
  staafdiagrammen (M/V/N/w) ongewijzigd.
  *Bestanden:* Modify `design-mockup/src/components/fem/FemCanvas.tsx`.
  *Niet aanraken:* engine.ts, App.tsx, FemProperties.tsx, core-bestanden.

- [ ] **P3.3 Randlast op een plaatrand.** Nieuw `LoadType` "edgeLoad": plateId +
  rand (onder/boven/links/rechts â€” de named edges van het gridmesh) + p (kN/m,
  richting x of z), invoer via het bestaande lastenpaneel, weergave op het canvas
  (pijltjesrij langs de rand), doorvoer App.tsx â†’ MultiInput â†’ in `buildMesh`
  omgezet met de PlateLoads-wrapper op de mesh-randknopen. Testinhoud
  (engine-level): trekwand belast met p op de bovenrand â†’ Î£F = pÂ·b binnen 1e-9
  relatief en u_top = Ïƒh/E binnen 1%; per belastinggeval gescheiden (randlast in
  geval 2 doet niets in geval 1).
  *Bestanden:* Modify `design-mockup/src/components/fem/femTypes.ts`,
  `design-mockup/src/components/fem/FemCanvas.tsx`, `design-mockup/src/App.tsx`,
  `design-mockup/src/components/fem/solver/types.ts`,
  `design-mockup/src/components/fem/solver/engine.ts`; Create
  `design-mockup/test-plaat-randlast.mjs`.
  *Niet aanraken:* FemProperties.tsx (aparte taak), TableView.tsx, core-bestanden.

## Fase P4 â€” Polygonen via randconforme CDT

- [ ] **P4.1 triangle-wasm werkend maken.** De door het project-CLAUDE.md
  aanbevolen API `generatePolygonPlateMeshV2` faalt nu runtime: `triangle-wasm`
  staat niet in design-mockup/package.json, `public/triangle.out.wasm` ontbreekt,
  en `design-mockup/src/core/triangle-wasm.d.ts` is een bewuste any-stub. Doen:
  npm-dependency toevoegen (root heeft ^1.0.0), wasm-bestand uit de root-public
  naar `design-mockup/public/`, stub vervangen door echte typedeclaraties, en
  verifiÃ«ren dat `vite build` het wasm-bestand in dist meeneemt Ã©n dat het pad
  (`new URL('/triangle.out.wasm', window.location.origin)`, TriangleService.ts:25)
  onder de gebouwde Tauri-app resolvet â€” expliciet risico, testen op de
  desktopbuild.
  *Test:* dev-smoke in de browserconsole: `triangulatePolygon` op een L-vorm
  levert een randconform mesh (alle outline-segmenten aanwezig als edges,
  segmentMarkers 1-based); `npm run build` bevat het wasm-bestand; Tauri-build
  start en meshet.
  *Bestanden:* Modify `design-mockup/package.json`,
  `design-mockup/src/core/triangle-wasm.d.ts`; Create (kopie)
  `design-mockup/public/triangle.out.wasm`.
  *Niet aanraken:* TriangleService.ts, PlateRegion.ts, alle UI-bestanden.

- [ ] **P4.2 Polygon-plaattool + async meshcache.** Tekentool uitbreiden: n hoeken
  klikken, sluiten door klik op de eerste knoop (Esc annuleert; HUD toont
  hoekenteller). Omdat CDT async is (WASM) en tussen versies niet bit-identiek
  hoeft te zijn: mesh **bij aanmaken/wijzigen** genereren via
  `generatePolygonPlateMeshV2`, als platte data (knopen + driehoeken +
  randknopen-per-rand) cachen op de Plate in de store en mee-serialiseren in het
  projectbestand (optioneel veld â€” oude bestanden laden ongewijzigd); de solve
  blijft synchroon en gebruikt de cache. Geometrie- of meshSize-wijziging
  invalideert en regenereert. Rechthoeken blijven het deterministische grid-pad
  gebruiken. Testinhoud: engine-level met een voorgebouwde cache
  (L-vormige schijf): patchtest â€” lineair verplaatsingsveld op de CDT-triangulatie
  exact (rel. fout < 1e-8) â€” en evenwicht Î£R = Î£F binnen 0,1%; round-trip van het
  cacheveld door serialize/deserialize.
  *Bestanden:* Modify `design-mockup/src/components/fem/femTypes.ts`,
  `design-mockup/src/hooks/useFemStore.ts`, `design-mockup/src/io/projectFile.ts`,
  `design-mockup/src/components/fem/FemCanvas.tsx`,
  `design-mockup/src/components/fem/solver/engine.ts`; Create
  `design-mockup/test-plaat-polygoon.mjs`.
  *Niet aanraken:* TriangleService.ts, PlateRegion.ts, App.tsx, FemProperties.tsx.

- [ ] **P4.3 Polygonvalidatie + nette foutpaden.** Zelfsnijdende polygonen en
  dubbele hoeken weigeren (pure validatiefunctie, unit-testbaar); mislukte
  WASM-init of CDT-fout â†’ NL-melding en gÃ©Ã©n half model (plaat niet aangemaakt,
  store ongewijzigd); randlast-UI (P3.3) werkt op polygonranden via rand-index
  in plaats van named edge. Testinhoud: validatiefunctie exact (vlinder-polygon
  geweigerd, convex/concaaf correct geaccepteerd, dubbele hoek geweigerd);
  foutpad handmatig (wasm hernoemen â†’ melding, model intact).
  *Bestanden:* Modify `design-mockup/src/components/fem/FemCanvas.tsx`,
  `design-mockup/src/components/fem/femTypes.ts` (edgeLoad rand-index); Create
  `design-mockup/test-plaat-validatie.mjs`.
  *Niet aanraken:* engine.ts (behalve edgeLoad-randindex-doorvoer indien nodig),
  core-bestanden.

## Fase P5 â€” Tabel en rapport

- [ ] **P5.1 Platen-tabel bewerkbaar.** `buildPlatesSpec` (TableView.tsx:356-373)
  is nu platte tekst; maak dikte/E/Î½/Ï/meshSize bewerkbaar zoals de knopen-/
  staaftabellen (â†’ `updatePlate` â†’ herberekening), plus kolommen aantal
  mesh-knopen/-elementen (uit de cache of het laatste resultaat). Geen add-row
  (tekenen gebeurt op het canvas). CSV-export dekt de nieuwe kolommen.
  *Test:* tsc schoon + handmatig: waarde in de tabel wijzigen verandert het
  resultaat; verwijderen via de tabel blijft werken.
  *Bestanden:* Modify `design-mockup/src/components/table/TableView.tsx`.
  *Niet aanraken:* engine.ts, App.tsx, FemCanvas.tsx, rapport-bestanden.

- [ ] **P5.2 Platen in het rapport.** `ReportData` (ReportDataContext.tsx:33-51)
  krijgt platen: invoersectie (tabel id/hoeken/dikte/materiaal E-Î½-Ï/meshSize/
  aantal elementen) en resultaatsectie per combinatie of omhullende (per plaat
  min/max van Ïƒx, Ïƒy, Ï„xy, von Mises + maatgevend element), aangesloten op het
  live rapportraamwerk (sectie-registry uit het HTML-rapportplan, met aan/uit).
  Contour-afbeeldingen in het rapport: backlog.
  *Test:* tsc schoon + handmatig: rapportwaarden matchen de canvas-legenda
  (zelfde min/max); bestaande secties en print-CSS ongewijzigd.
  *Bestanden:* Modify
  `design-mockup/src/components/report/ReportDataContext.tsx`,
  `design-mockup/src/App.tsx` (doorvoer), nieuwe sectie-component onder
  `design-mockup/src/components/report/`.
  *Niet aanraken:* engine.ts, FemCanvas.tsx, TableView.tsx, core-bestanden.

---

## Backlog (bewust niet in fase 1)

- **Plaatbuiging in de UI** (vloer/uit het vlak): DKT is na P0.2 correct, maar
  vergt een eigen analyse-/resultatenwereld (w, mx/my/mxy, oplegranden) en doet in
  mixed-mode niet mee; ook quads worden in `plate_bending` stil overgeslagen
  (Assembler.ts:160) â€” dat pad verdient dan een waarschuwing of quad-DKT.
- **Normtoetsing platen** (plooi/wandtoetsen) en **wapening/beton** â€” `PlateFNL.ts`
  (gelaagd gekraakt model) is nu dead code; activeren of verwijderen beslist bij
  die feature.
- **Sparingen/gaten**: voids zitten al in de CDT-service (interieurpunten,
  TriangleService.ts:236-278) maar UI + validatie + randlast-op-gatrand is een
  eigen traject.
- **Vlaklast kN/mÂ²** uit het vlak bestaat niet in het 2D-in-vlak-model; in het
  vlak is eigengewicht gedekt (P2.3).
- **Platen + 2e orde**: `solveMixed` is puur lineair; geometrische
  membraanstijfheid + koppeling met het P-Î”-pad is nieuwbouw.
- **Sparse/banded solver**: dense Gauss is de architecturale grens (Â±4000 DOF's);
  nodig voor fijne meshes en grote wanden.
- **Remesh bij verslepen** van hoekknopen (v1 had remesh-orkestratie in de
  MeshEditor); fase 1 regenereert de polygon-cache alleen expliciet.
- **Quad-pairing voor CDT-meshes** (TriToQuad.ts bestaat, ongebruikt) â€” betere
  nauwkeurigheid per DOF dan CST.
- **Contour-afbeelding in het rapport** (canvas-snapshot in de resultaatsectie).
- **Opschonen magic numbers** in het plaat-only pad (0,5 m constraint-transfer,
  rotatiestabilisatie 1e-6) â€” fase 1 gebruikt dat pad niet (mixed valideert hard),
  maar `solvePlateOrPlane` blijft bestaan.
