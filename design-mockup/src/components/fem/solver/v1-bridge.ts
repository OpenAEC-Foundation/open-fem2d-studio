/**
 * DEPRECATED — name kept only as a one-line re-export so any straggling
 * import paths don't break. The FEM engine lives in `engine.ts` (own code).
 * This file will be removed once all imports point to `engine.ts` directly.
 */
export * from "./engine";
